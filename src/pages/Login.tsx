import { IonContent, IonHeader, IonPage, IonTitle, IonToolbar, IonGrid, IonRow, IonCol } from '@ionic/react';
import './Tab1.css';

const Login: React.FC = () => {
  return (
    <IonContent>
      <IonHeader>
        <IonTitle>Welcome to Sport Score</IonTitle>
      </IonHeader>
    </IonContent>
  );
};

export default Login;
